import 'package:flutter/material.dart';

class AppColors {
  // Primary Brand Color
  static const Color primary = Color(0xFFFF2A5F);

  // Secondary Color (لو عندك لون مساعد للكروت أو الـ icons)
  static const Color secondary = Color(0xFF1E1E2C);

  // Background Colors
  static const Color scaffoldBackground = Color(0xFFF8F9FB);
  static const Color cardBackground = Colors.white;

  // Text Colors
  static const Color textPrimary = Color(0xFF1A1A1A);
  static const Color textSecondary = Color(0xFF6B6B6B);

  // Icon Colors
  static const Color iconPrimary = primary;

  // Border / Divider
  static const Color border = Color(0xFFE5E5E5);

  // Danger Colors
  static const Color danger = Color(0xFFE53935);
  static const Color dangerLight = Color(0xFFFFEBEE);
  static const Color dangerDark = Color(0xFFB71C1C);
}
